// Kept as a thin re-export so existing imports keep working; the renderer body now
// lives in the tool registry (GenericTool is the fallback renderer).
export { GenericTool as ToolCallCard } from './tools/GenericTool'
