# Copy to `.env.local` in this folder (gitignored). Electron main loads these on startup.
COMPOSIO_API_KEY=
# Optional: absolute path to Pi’s shared Composio automations JSON (default: ~/.config/pi/composio-automations.json)
# PI_COMPOSIO_AUTOMATIONS_JSON=
